# Axel Engineering OS Core

This package contains the portable core runtime installed by `axel-agents`: agents, shared skills, workflows, checks, templates, memory schemas, commands, hooks, rules, and documentation.

The installer writes these assets to the root selected by an adapter and generates `config.yaml`. Framework packs add skills after core installation. Use the installed `commands/` contracts and Evidence Report template to enforce selective loading and evidence-first completion.
