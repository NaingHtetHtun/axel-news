# Agents

Agents define role, authority, and default skill loading; they are not autonomous permission grants. Select the narrowest role that owns the task, resolve its required skill manifests, then apply project rules. When responsibilities overlap, use one primary agent and request a focused review from the other role.
