# Commands

Commands are model-agnostic task entry points. A host may map slash commands to these files, but each command remains useful when pasted as instructions. Commands select agents and orchestration; they do not replace user authority or project-specific tool configuration.
