# /deploy

Select `devops`; load `release-workflow`, release-relevant memory, and `release-safety`.

1. Verify artifact, configuration, migration sequence, owner, monitoring, and rollback.
2. Run `release-readiness` and `evidence-first` gates.
3. Deploy only with explicit user authority and record observed post-release evidence.
