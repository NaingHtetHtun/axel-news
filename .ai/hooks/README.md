# Hooks

Hooks are lifecycle contracts a host agent can invoke around work. They are declarative and portable: OpenCode, Codex, Claude Code, or another host can map them to native events without changing the Engineering OS policy.
