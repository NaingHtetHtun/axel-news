# Prompt Entry Points

Prompts are concise invocation templates, not hidden policy. Replace bracketed placeholders, then let the agent resolve the workflows and skill manifests named by the prompt. Add project-specific prompts only when they compose existing skills and rules rather than duplicating them.
