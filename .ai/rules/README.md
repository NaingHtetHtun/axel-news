# Rules

Rules are concise, cross-cutting constraints that apply by default. They are not a substitute for skills: use `core-standards.md` as the policy index, then load the named skill for implementation guidance. Add project overrides as separate dated files with rationale, owner, scope, and expiry/review date.
