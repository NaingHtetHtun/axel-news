# Debug Workflow

Load `debugging-method`, `logging`, and `observability`.

1. Establish a timeline and correlation ID.
2. Compare successful and failing paths at the first divergence.
3. Narrow the trust or state boundary that first violates expectation.
4. Verify one hypothesis at a time with reproducible evidence.
5. Hand off to `bug-fix` when a cause is confirmed.
