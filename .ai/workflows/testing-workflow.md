# Testing Workflow

Load `testing-strategy`, then the relevant domain skill.

1. Derive behaviors, boundaries, and risks from the requested change.
2. Select the lowest useful test levels.
3. Cover success, edge, failure, authorization, and compatibility cases as relevant.
4. Make fixtures deterministic and assertions observable.
5. Run focused tests first, then required broader checks.
